---
name: Colors
---

import { Playground } from 'docz'
import { Box, Boxer, Typography } from 'scaleway-ui'
import * as colors from 'theming/colors'

# Colors

<Playground>
  <Boxer>
    {Object.keys(colors).map((color, i) => {
      return color !== "colors" && (
        <Box display="flex" justifyContent="center">
          <Box textAlign="center" width={100} p={1} m={1} backgroundColor="black">
            <Typography key={`${color}-black`} color={color}>
                {color}
            </Typography>
          </Box>
          <Box textAlign="center" width={100} p={1} m={1} backgroundColor="white">
            <Typography key={`${color}-white`} color={color}>
                {color}
            </Typography>
          </Box>
        </Box>
      )
    })}
  </Boxer>
</Playground>
