---
name: RichSelect
menu: Components
---

import { useState, useCallback } from 'react'
import { Playground } from 'docz'
import { RichSelect } from 'components/RichSelect'
import { ErrorTransition } from './ErrorTransition'

# RichSelect

## Uncontrolled

RichSelect can be used as an [uncontrolled component](https://reactjs.org/docs/uncontrolled-components.html).

<Playground>
  <RichSelect>
    <RichSelect.Option value="a">Option A</RichSelect.Option>
    <RichSelect.Option value="b">Option B</RichSelect.Option>
  </RichSelect>
</Playground>

## Controlled

Most of the time, you need a [controlled component](https://reactjs.org/docs/forms.html).

<Playground>
  {() => {
    const [value, setValue] = useState({ value: 'a', label: 'Option A' })
    const handleChange = useCallback(
      value => setValue(value),
      [],
    )
    return (
      <RichSelect value={value} onChange={handleChange}>
        <RichSelect.Option value="a">Option A</RichSelect.Option>
        <RichSelect.Option value="b">Option B</RichSelect.Option>
      </RichSelect>
    )
  }}
</Playground>

## Disabled

Mark `RichSelect` as disabled using `disabled` property.

<Playground>
  <RichSelect disabled>
    <RichSelect.Option value="a">Option A</RichSelect.Option>
    <RichSelect.Option value="b">Option B</RichSelect.Option>
  </RichSelect>
</Playground>

## Time

Mark `RichSelect` as time selector using `time` property.

<Playground>
  <RichSelect time>
    <RichSelect.Option value="11">11:00</RichSelect.Option>
    <RichSelect.Option value="12">12:00</RichSelect.Option>
  </RichSelect>
</Playground>

## Required

Mark `RichSelect` as required using `required` property.

<Playground>
  <RichSelect required>
    <RichSelect.Option value="a">Option A</RichSelect.Option>
    <RichSelect.Option value="b">Option B</RichSelect.Option>
  </RichSelect>
</Playground>

## Error

Mark `RichSelect` as errored using `error` property.

<Playground>
  <RichSelect error>
    <RichSelect.Option value="a">Option A</RichSelect.Option>
    <RichSelect.Option value="b">Option B</RichSelect.Option>
  </RichSelect>
</Playground>

## Time on error

<Playground>
  <RichSelect time error>
    <RichSelect.Option value="11">11:00</RichSelect.Option>
    <RichSelect.Option value="12">12:00</RichSelect.Option>
  </RichSelect>
</Playground>

## Error message

Mark `RichSelect` as errored using `error` property with a message.

<Playground>
  <ErrorTransition
      error="An error"
      Component={RichSelect}
  />
</Playground>
