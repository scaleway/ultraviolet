---
name: Badge
menu: Components
---

import { Playground, Props } from 'docz'
import { Badge, Boxer } from 'scaleway-ui'

# Badge

## Basic

<Playground>
  <Badge>A badge</Badge>
</Playground>

## Variants

Set variant using `variant` property.

<Playground>
  <Boxer my={1}>
    <Badge variant="neutral">neutral (default)</Badge>
    <Badge variant="success">success</Badge>
    <Badge variant="beta">beta</Badge>
    <Badge variant="warning">warning</Badge>
  </Boxer>
</Playground>

## Size

Set variant using `size` property.

<Playground>
  <Boxer my={1}>
    <Badge>md (default)</Badge>
    <Badge size="sm">sm</Badge>
    <Badge size="xs">xs</Badge>
    <Badge size="xxs">xxs</Badge>
  </Boxer>
</Playground>
