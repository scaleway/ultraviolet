---
name: Toaster
menu: Components
---

import { Playground } from 'docz'
import { Boxer, Button, toast, ToastContainer } from 'scaleway-ui'

<ToastContainer />

# Toaster

## Basic

<Playground>
  <Boxer>
    <Button mb={1} onClick={() => toast.success('This is success')}>Success</Button>
    <Button mb={1} onClick={() => toast.info('This is info')}>Info</Button>
    <Button mb={1} onClick={() => toast.warn('This is warn')}>Warn</Button>
    <Button mb={1} onClick={() => toast.error('This is error')}>Error</Button>
  </Boxer>
</Playground>
