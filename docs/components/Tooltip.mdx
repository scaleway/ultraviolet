---
name: Tooltip
menu: Components
---

import { Playground } from 'docz'
import { Box, Button, Tooltip } from 'scaleway-ui'

# Tooltip

Displays a tooltip on mouse over.

## Default

<Playground>
  <Box display="flex" flexDirection="column">
    <Box display="flex" justifyContent="space-evenly">
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip text="tooltip">Tooltip around the text</Tooltip>
      </Box>
      <Tooltip text="tooltip">
        <Box
          borderRadius={20}
          border="2px solid #333"
          p={4}
          width="max-content"
        >
          Tooltip around the box
        </Box>
      </Tooltip>
    </Box>

    <Box mt={2} display="flex" justifyContent="space-evenly">
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip text="tooltip">
          {referenceProps => <div {...referenceProps}>I'm a function</div>}
        </Tooltip>
      </Box>
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip text="tooltip">
          <Button width={220} onClick={() => alert('fired!')}>
            Should be fired on click
          </Button>
        </Tooltip>
        <Box mt={2}>
          <Tooltip text="tooltip">
            <Button width={220} disabled onClick={() => alert('fired!')}>
              Should not be fired on click
            </Button>
          </Tooltip>
        </Box>
      </Box>
    </Box>

    <Box mt={2} display="flex" justifyContent="space-evenly">
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip text="tooltip">
          <Box>
            <div>I'm a nested element</div>
          </Box>
        </Tooltip>
      </Box>
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip text="tooltip">
          {referenceProps => (
            <Box {...referenceProps}>
              I'm a nested function, with naked string children
            </Box>
          )}
        </Tooltip>
      </Box>
    </Box>

    <Box mt={2} display="flex" justifyContent="space-evenly">
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip text="tooltip" backgroundColor="red">
          <div>I'm a customized tooltip</div>
        </Tooltip>
      </Box>
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip width={150} text="I have a very very very very very very very very very very very very very very long text">
          <div>I'm a long tooltipp</div>
        </Tooltip>
      </Box>
    </Box>

    <Box mt={2} display="flex" justifyContent="space-evenly">
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip text="empty div">
          <div style={{ width: 10, height: 10, backgroundColor: 'red' }} />
        </Tooltip>
      </Box>
      <Box borderRadius={20} border="2px solid #333" p={4} width="max-content">
        <Tooltip text="tooltip" backgroundColor="red">
          <>
            <div>I'm the first children</div>
            <div>I'm the second children</div>
            <div>Tooltip can only have one children, not an array of children. Do not forget to wrap it with fragment</div>
          </>
        </Tooltip>
      </Box>
    </Box>

  </Box>
</Playground>

## Reakit tooltip Placement

Set placement using `placement` prop.

<Playground>
  <Box display="flex" justifyContent="space-evenly">
    {['left', 'top', 'bottom', 'right'].map(placement => (
      <Box key={placement} p={2}>
        <Tooltip text="tooltip" placement={placement}>
          {placement}
        </Tooltip>
      </Box>
    ))}
  </Box>
</Playground>

## API

See list of props [here](https://reakit.io/docs/tooltip/)
