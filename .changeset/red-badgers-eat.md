---
"@ultraviolet/ui": patch
---

`SelectInput`: dropdown should resize with select bar

`OptionSelector`: new prop `direction` and `hideWhenEmpty`, various style improvements and enhanced `onChange` function to indicate which selector triggered the change (first or second selector)