import { Template } from './Template.stories'

export const CustomTexts = Template.bind({})

CustomTexts.args = {
  copyText: 'Click on me to copy the text',
  copiedText: "That's it! Text copied",
}
