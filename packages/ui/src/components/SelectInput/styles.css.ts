import { theme } from '@ultraviolet/themes'
import { style } from '@vanilla-extract/css'

export const selectinputContainer = style({ width: '100%' })
export const selectinputHelper = style({ paddingTop: theme.space['0.5'] })
