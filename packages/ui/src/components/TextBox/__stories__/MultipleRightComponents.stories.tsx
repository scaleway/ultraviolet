import { Template } from './Template.stories'

export const MultipleRightComponents = Template.bind({})

MultipleRightComponents.args = {
  label: 'Multiple',
  random: 'prefix',
  unit: 'px',
  required: true,
  type: 'toggleable-password',
  valid: true,
}
