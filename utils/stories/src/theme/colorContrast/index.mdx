import { Meta } from '@storybook/addon-docs/blocks'
import { ContrastChecker } from './ContrastChecker'

<Meta title="Customization/Color Contrast" />

# Color Contrast Checker

Displays WCAG contrast ratios for intended text-on-background pairings (matching state & variant
suffixes) within each sentiment. Use the theme switcher in the toolbar to check light, dark, and
darker themes.

<ContrastChecker />
