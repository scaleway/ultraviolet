import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'
import Range from '..'
import ControlValue from '../../../__stories__/components/ControlValue'
import Box from '../../Box'

<Meta title="Components/Data Entry/Range" component={Range} />

# Range

## Controlled

<Description>A range bar </Description>

Range only work as [controlled component](https://reactjs.org/docs/forms.html).

<Canvas>
  <Story name="Basic" height="200px">
    <Box mt={3}>
      <Range />
    </Box>
  </Story>
</Canvas>

## Single value

<Canvas>
  <Story name="Single value" height="150px">
    <Box mt={3}>
      <ControlValue value={[3]}>
        {({ value, onChange }) => (
          <Range value={value} min={0} max={10} onChange={onChange} />
        )}
      </ControlValue>
    </Box>
  </Story>
</Canvas>

## Two values

For now, Range component will work with max two values.

<Canvas>
  <Story name="Two values" height="150px">
    <Box mt={3}>
      <ControlValue value={[3, 8]}>
        {({ value, onChange }) => (
          <Range value={value} min={0} max={15} onChange={onChange} />
        )}
      </ControlValue>
    </Box>
  </Story>
</Canvas>

## API

<ArgsTable of={Range} />
