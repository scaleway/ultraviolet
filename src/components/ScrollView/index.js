import styled from '@emotion/styled'
import { Box } from '../Box'

const ScrollView = styled(Box)`
  display: flex;
  flex-direction: column;
  width: 100%;
  overflow-y: auto;
`

export { ScrollView }
