import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import { Action } from '..'
import { buttonVariants } from '../../Button'
import { icons } from '../../Icon'

<Meta title="Components/Action" component={Action} />

# Action

## Basic

<Canvas>
  <Story name="basic">
    <Action name="lock" />
  </Story>
</Canvas>

## Variant

<Canvas>
  <Story name="variant">
    {buttonVariants.map(variant => (
      <Action name="lock" variant={variant} mr={2} />
    ))}
  </Story>
</Canvas>

## Size

<Description>Set size using `size` property.</Description>
<Description>size will throw an error if size &gt; 32 </Description>

<Canvas>
  <Story name="size">
    {[10, 18, 24, 32].map(size => (
      <Action key={size} name="lock" variant="secondary" size={size} mr={2} />
    ))}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="disabled">
    <Action name="lock" mr={2} disabled />
  </Story>
</Canvas>

## Icon

<Description>You can change the icon by setting `name` prop.</Description>

<Canvas>
  <Story name="icon">
    {icons.map(icon => (
      <Action key={icon} name={icon} mr={2} mb={2} />
    ))}
  </Story>
</Canvas>

## Rounded

<Canvas>
  <Story name="rounded">
    <Action name="lock" rounded />
  </Story>
</Canvas>

## Tooltip

<Description>Set tooltip using `tooltip` property.</Description>

<Canvas>
  <Story name="tooltip">
    <Action name="lock" variant="primary" tooltip="I am locked" />
  </Story>
</Canvas>

## String

<Description>
  You can fill action button with text by adding it in `children`. You will also
  need to adjust width value.
</Description>

<Canvas>
  <Story name="string">
    <Action variant="secondary-bordered" width={32}>
      hello
    </Action>
  </Story>
</Canvas>

<ArgsTable of={Action} />
