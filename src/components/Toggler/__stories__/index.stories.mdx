import {
  Meta,
  Story,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks'
import { Toggler } from '..'
import { Button } from 'components'

<Meta title="Components/Toggler" component={Toggler} />

# Toggler

<Description>
  Toggler, see [Smooth UI Toggler](https://smooth-ui.smooth-code.com/docs-utilities-toggler).
</Description>

## Basic

<Canvas>
  <Story name="basic">
    <Toggler>
      {([toggled, toggle]) => (
        <Button onClick={() => toggle()}>{toggled ? 'ON' : 'OFF'}</Button>
      )}
    </Toggler>
  </Story>
</Canvas>
