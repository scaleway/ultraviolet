import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'
import StatusIndicator from '..'

<Meta
  title="Components/Data Display/StatusIndicator"
  component={StatusIndicator}
/>

# StatusIndicator

<Description>Colorful circle that can be used to indicated state.</Description>

<Canvas>
  <Story name="Basic">
    <StatusIndicator status="available" />
  </Story>
</Canvas>

## Status

<Description>
  Status prop can be used to show different state types and color.
</Description>

<Canvas>
  <Story name="Status">
    <StatusIndicator status="available" mr={1} />
    <StatusIndicator status="unavailable" mr={1} />
    <StatusIndicator status="available" mr={1} />
    <StatusIndicator status="starting" mr={1} />
    <StatusIndicator status="stopped_in_place" mr={1} />
  </Story>
</Canvas>

## Animated

<Description>
  Animated prop will make StatusIndicator blink. This prop is usually used for
  temporary or "in progress" state.
</Description>

<Canvas>
  <Story name="Animated">
    <StatusIndicator status="starting" tooltip="Starting animated" animated />
  </Story>
</Canvas>

## Custom status

<Canvas>
  <Story name="Custom status">
    {() => {
      const statuses = {
        fake_status: 'purple',
      }
      return <StatusIndicator status="fake_status" statuses={statuses} />
    }}
  </Story>
</Canvas>

<ArgsTable of={StatusIndicator} />
