import {
  Meta,
  Story,
  Canvas,
  Description,
} from '@storybook/addon-docs/blocks'
import { Icon, icons } from '..'
import { Box, Boxer } from 'components'

<Meta title="Components/Icon" component={Icon} />

# Icon

## Basic

<Description>
  Set desired icon using `name` property.
</Description>

<Canvas>
  <Story name="basic">
    {icons.map(name => (
      <Box my={1} key={name} style={{ fontSize: '1.3em' }}>
        <Icon name={name} /> {name}
      </Box>
    ))}
  </Story>
</Canvas>

## Size

<Description>
  Set size using `size` property.
</Description>

<Canvas>
  <Story name="size">
    <Boxer mr={2} display="inline-block">
      <Icon name="eye" size={40} />
      <Icon name="eye" size={50} />
      <Icon name="eye" size={60} />
    </Boxer>
  </Story>
</Canvas>

## Color

<Description>
  Set color using `color` property.
</Description>

<Canvas>
  <Story name="color">
    <Boxer mr={2} display="inline-block">
      <Icon name="eye" color="primary" />
      <Icon name="eye" color="success" />
      <Icon name="eye" color="warning" />
    </Boxer>
  </Story>
</Canvas>

## Unknown or `undefined` name case

<Description>
  If name is `undefined` or not found warning is sent and default circle icon is displayed.
</Description>

<Canvas>
  <Story name="default">
    <Boxer mr={2} display="inline-block">
      <Icon name={undefined} color="red" />
      <Icon name="unknown" color="orange" />
    </Boxer>
  </Story>
</Canvas>
