import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import MenuIcon, { availableIcons } from '..'
import Box from '../../Box'

<Meta title="Components/MenuIcon" component={MenuIcon} />

# MenuIcon

<Description>
  MenuIcon is an icon component styled to fit into menus
</Description>

<Canvas>
  <Story name="Basic">
    {availableIcons.map(icon => (
      <Box key={icon} display="flex">
        <MenuIcon name={icon} />
        {icon}
      </Box>
    ))}
  </Story>
</Canvas>

<ArgsTable of={MenuIcon} />
