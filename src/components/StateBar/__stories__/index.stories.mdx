import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'
import StateBar, { Bar, State } from '..'

<Meta title="Components/Data Display/StateBar" component={StateBar} />

# StateBar

<Description>A description of a progress bar.</Description>

<Canvas>
  <Story name="Basic">
    <StateBar my={2}>
      <StateBar.State label="Label">your value</StateBar.State>
      <StateBar.Bar value={50} />
    </StateBar>
    <StateBar my={2}>
      <StateBar.State label="Value >= 90" />
      <StateBar.Bar value={90} />
    </StateBar>
  </Story>
</Canvas>

## Unlimited

<Description>
  Set unlimited using `unlimited` prop on `StateBar.Bar`.
</Description>

<Canvas>
  <Story name="Unlimited">
    <StateBar my={2}>
      <StateBar.State label="Unlimited" />
      <StateBar.Bar unlimited />
    </StateBar>
  </Story>
</Canvas>

## Progress

<Canvas>
  <Story name="Progress">
    <StateBar my={2}>
      <StateBar.State label="Progress" />
      <StateBar.Bar progress />
    </StateBar>
  </Story>
</Canvas>

## API

### `StateBar`

<ArgsTable of={StateBar} />

### `StateBar.State`

<ArgsTable of={State} />

### `StateBar.Bar`

<ArgsTable of={Bar} />
