import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import { Grid } from '..'

<Meta title="Components/Grid" component={Grid} />

# Grid

<Description>
  `Grid`, `Row` and `Col` are exported from Smooth UI. See [Smooth UI grid
  documentation](https://smooth-ui.smooth-code.com/docs-basics-grid).
</Description>

<Canvas>
  <Story name="default">
    <Grid />
  </Story>
</Canvas>

<ArgsTable of={Grid} />
