import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import CategoryIcon, { availableIcons } from '..'
import Box from '../../Box'

<Meta title="Components/CategoryIcon" component={CategoryIcon} />

# CategoryIcon

<Canvas>
  <Story name="Basic">
    {availableIcons.map(name => (
      <Box
        display="flex"
        alignItems="center"
        my={1}
        key={name}
        style={{ fontSize: '1.3em' }}
      >
        <CategoryIcon name={name} mr={2} />
        {name}
      </Box>
    ))}
  </Story>
</Canvas>

## Size

<Description>
  You can change the size of the icon using `size` properties.
</Description>

<Canvas>
  <Story name="Size">
    {availableIcons.map(name => (
      <Box
        display="flex"
        alignItems="center"
        my={1}
        key={name}
        style={{ fontSize: '1.3em' }}
        mb={2}
      >
        <CategoryIcon size={64} name={name} mr={2} />
        {name}
      </Box>
    ))}
  </Story>
</Canvas>

## API

<ArgsTable of={CategoryIcon} />
