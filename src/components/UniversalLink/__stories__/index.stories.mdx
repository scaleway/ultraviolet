import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { BrowserRouter, Link } from 'react-router-dom'
import UniversalLink from '..'

<Meta title="Components/UniversalLink" component={UniversalLink} />

# UniversalLink

A basic link component to display beautiful links.

<Canvas>
  <Story name="Basic">
    <UniversalLink href="/?path=/docs/components-universallink--basic#basic">
      href
    </UniversalLink>
  </Story>
</Canvas>

## Custom Link Component

Use `to` prop to use the linkComponent that come from the `ThemeProvider`. If you don't set it in your theme you can use `as` prop the change the component that will be rendered.

<Canvas>
  <Story name="Custom Link Component">
    <BrowserRouter>
      <UniversalLink
        to="?path=/docs/components-universallink--basic#with-react-router"
        as={Link}
      >
        to
      </UniversalLink>
    </BrowserRouter>
  </Story>
</Canvas>

## API

<ArgsTable of={UniversalLink} />
