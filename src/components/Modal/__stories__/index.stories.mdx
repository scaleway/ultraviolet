import { css } from '@emotion/react'
import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import { Box, Button, Modal } from '../..'

<Meta title="Components/Modal" component={Modal} />

# Modal

## Basic Usage

<Description>
  Documentation is here https://reakit.io/docs/dialog/#props By default now our
  modal is a portal. If you want to change this you can disabled modal props but
  be aware of https://reakit.io/docs/dialog/#non-modal-dialogs
</Description>

<Canvas>
  <Story name="basic">
    <Modal disclosure={() => <Button>Open Modal</Button>}>
      <Box p={4}>Content should be present in center of the modal</Box>
    </Modal>
  </Story>
</Canvas>

<Canvas>
  <Story name="basic-with-much-content">
    <Modal
      animated
      animation="scaleUp"
      disclosure={() => <Button>Open Modal with lot of content</Button>}
    >
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
      <Box p={4}>Content should be present in center of the modal</Box>
    </Modal>
  </Story>
</Canvas>

## Animated

<Canvas>
  <Story name="animated fold">
    <Modal
      animated
      animation="fold"
      disclosure={() => <Button>fold Modal</Button>}
    >
      <Box p={4}>Modal should be animated</Box>
    </Modal>
  </Story>
  <Story name="animated zoom">
    <Modal
      animated
      animation="zoom"
      disclosure={() => <Button>zoom Modal</Button>}
    >
      <Box p={4}>Modal should be animated</Box>
    </Modal>
  </Story>
  <Story name="animated scaleUp">
    <Modal
      animated
      animation="scaleUp"
      disclosure={() => <Button>scaleUp Modal</Button>}
    >
      <Box p={4}>Modal should be animated</Box>
    </Modal>
  </Story>
  <Story name="animated scaleBack">
    <Modal
      animated
      animation="scaleBack"
      disclosure={() => <Button>scaleBack Modal</Button>}
    >
      <Box p={4}>Modal should be animated with scaleBack effect</Box>
    </Modal>
  </Story>
  <Story name="animated sketch">
    <Modal
      animated
      animation="sketch"
      disclosure={() => <Button>sketch</Button>}
    >
      <Box p={4}>Modal should be animated with sketch effect</Box>
    </Modal>
  </Story>
  <Story name="animated slideTop">
    <Modal
      animated
      animation="slideTop"
      disclosure={() => <Button>slideTop</Button>}
      placement="top"
    >
      <Box p={4}>Modal should be animated with slideTop animation</Box>
    </Modal>
  </Story>
  <Story name="animated slideBottom">
    <Modal
      animated
      animation="slideBottom"
      disclosure={() => <Button>slideBottom</Button>}
      placement="bottom"
    >
      <Box p={4}>Modal should be animated with slideBottom animation</Box>
    </Modal>
  </Story>
  <Story name="animated slideLeft">
    <Modal
      animated
      animation="slideLeft"
      disclosure={() => <Button>slideLeft</Button>}
      placement="left"
    >
      <Box p={4}>Modal should be animated with slideLeft animation</Box>
    </Modal>
  </Story>
  <Story name="animated slideRight">
    <Modal
      animated
      animation="slideRight"
      disclosure={() => <Button>slideRight</Button>}
      placement="right"
    >
      <Box p={4}>Modal should be animated with slideRight animation</Box>
    </Modal>
  </Story>
</Canvas>

## Width

<Description>
  we accept a size props xxsmall, xsmall, small, medium, large
</Description>

<Canvas>
  <Story name="Width xssmall">
    <Modal width="xxsmall">
      <Box p={4}>Content should be present in center of the modal</Box>
    </Modal>
  </Story>
  <Story name="Width xsmall">
    <Modal disclosure={() => <Button>xsmall</Button>} width="xsmall">
      <Box p={4}>Content should be present in center of the modal</Box>
    </Modal>
  </Story>
  <Story name="Width small">
    <Modal disclosure={() => <Button>small</Button>} width="small">
      <Box p={4}>Content should be present in center of the modal</Box>
    </Modal>
  </Story>
  <Story name="Width medium">
    <Modal disclosure={() => <Button>medium</Button>} width="medium">
      <Box p={4}>Content should be present in center of the modal</Box>
    </Modal>
  </Story>
  <Story name="Width large">
    <Modal disclosure={() => <Button>large</Button>} width="large">
      <Box p={4}>Content should be present in center of the modal</Box>
    </Modal>
  </Story>
</Canvas>

## Placement

<Description>
  placement center, top, top-right, right, bottom-right,bottom ,bottom-left,
  leftright, center, top, bottom
</Description>

<Canvas>
  <Story name="center">
    <Modal
      disclosure={() => <Button>center</Button>}
      placement="center"
      width="xsmall"
    >
      <Box p={4}>This modal should be center</Box>
    </Modal>
  </Story>
  <Story name="top-left">
    <Modal
      disclosure={() => <Button>top-left</Button>}
      placement="top-left"
      width="xsmall"
    >
      <Box p={4}>This modal should be place on top-left</Box>
    </Modal>
  </Story>
  <Story name="top">
    <Modal
      disclosure={() => <Button>top</Button>}
      placement="top"
      width="xsmall"
    >
      <Box p={4}>This modal should be top</Box>
    </Modal>
  </Story>
  <Story name="top-right">
    <Modal
      disclosure={() => <Button>top-right</Button>}
      placement="top-right"
      width="xsmall"
    >
      <Box p={4}>This modal should be place on top-right</Box>
    </Modal>
  </Story>
  <Story name="right">
    <Modal
      disclosure={() => <Button>right</Button>}
      placement="right"
      width="xsmall"
    >
      <Box p={4}>This modal should be place on right</Box>
    </Modal>
  </Story>
  <Story name="bottom-right">
    <Modal
      disclosure={() => <Button>bottom-right</Button>}
      placement="bottom-right"
      width="xsmall"
    >
      <Box p={4}>This modal should be place on bottom-right</Box>
    </Modal>
  </Story>
  <Story name="bottom">
    <Modal
      disclosure={() => <Button>bottom</Button>}
      placement="bottom"
      width="xsmall"
    >
      <Box p={4}>This modal should be place on bottom</Box>
    </Modal>
  </Story>
  <Story name="bottom-left">
    <Modal
      disclosure={() => <Button>bottom-left</Button>}
      placement="bottom-left"
      width="xsmall"
    >
      <Box p={4}>This modal should be place on bottom-left</Box>
    </Modal>
  </Story>
  <Story name="left">
    <Modal
      disclosure={() => <Button>left</Button>}
      placement="left"
      width="xsmall"
    >
      <Box p={4}>This modal should be place on left</Box>
    </Modal>
  </Story>
</Canvas>

## height

<Canvas>
  <Story name="height right xsmall">
    <Modal
      disclosure={() => <Button>height right xsmall</Button>}
      width="xsmall"
      height="100%"
      placement="right"
    >
      <Box p={4}>Hello there this a modal with a full height on the right</Box>
    </Modal>
  </Story>
  <Story name="height left xsmall">
    <Modal
      disclosure={() => <Button>height left xsmall</Button>}
      width="xsmall"
      height="100%"
      placement="left"
    >
      <Box p={4}>Hello there this a modal with a full height on the left</Box>
    </Modal>
  </Story>
  <Story name="height right small">
    <Modal
      disclosure={() => <Button>height right small</Button>}
      width="small"
      height="100%"
      placement="right"
    >
      <Box p={4}>Hello there this a modal with a full height on the right</Box>
    </Modal>
  </Story>
  <Story name="height right medium">
    <Modal
      disclosure={() => <Button>height right medium</Button>}
      width="medium"
      height="100%"
      placement="right"
    >
      <Box p={4}>Hello there this a modal with a full height on the right</Box>
    </Modal>
  </Story>
  <Story name="height right large">
    <Modal
      disclosure={() => <Button>height right large</Button>}
      width="large"
      height="100%"
      placement="right"
    >
      <Box p={4}>Hello there this a modal with a full height on the right</Box>
    </Modal>
  </Story>
</Canvas>

## props

<Canvas>
  <Story name="preventBodyScroll">
    <Modal
      disclosure={() => <Button>preventBodyScroll</Button>}
      width="xsmall"
      preventBodyScroll={false}
    >
      <Box p={4}>
        Try to scroll on body ( outside of the Modal ) preventBodyScroll is
        available only when modal props is enabled
        https://reakit.io/docs/dialog/#props
      </Box>
    </Modal>
  </Story>
  <Story name="hideOnClickOutside">
    <Modal
      disclosure={() => <Button>hideOnClickOutside</Button>}
      width="small"
      hideOnClickOutside={false}
    >
      <Box p={4}>Try to click outside of the Modal</Box>
    </Modal>
  </Story>
  <Story name="hideOnEsc">
    <Modal
      disclosure={() => <Button>hideOnEsc</Button>}
      width="medium"
      hideOnEsc={false}
    >
      <Box p={4}>try to ESCAPE</Box>
    </Modal>
  </Story>
  <Story name="modal">
    <Modal
      disclosure={() => <Button>modal</Button>}
      width="medium"
      modal={false}
    >
      <Box p={4}>Our modal is juste below our Button in the DOM </Box>
    </Modal>
  </Story>
</Canvas>

## isClosable

<Canvas>
  <Story name="isClosable">
    <Modal disclosure={() => <Button>modal</Button>} isClosable={false}>
      {({ hide }) => (
        <>
          <p>You don&apos;t have a close Icon</p>
          <Button onClick={() => hide()}>Close</Button>
        </>
      )}
    </Modal>
  </Story>
</Canvas>

## Bordered

<Canvas>
  <Story name="bordered">
    <Modal disclosure={() => <Button>modal</Button>} bordered={false}>
      <p>Your Modal is not bordered</p>
    </Modal>
  </Story>
</Canvas>

## Custom Styles

<Canvas>
  <Story name="custom style">
    {() => {
      const customDialogBackdropStyles = css`
        background-color: aliceblue;
      `
      const customDialogStyles = css`
        background: radial-gradient(
          circle,
          #8b2fe6 0%,
          #4f0599 50%,
          #30015a 100%
        );
      `
      return (
        <Modal
          disclosure={() => <Button>modal</Button>}
          bordered={false}
          customDialogBackdropStyles={customDialogBackdropStyles}
          customDialogStyles={customDialogStyles}
        >
          <p>Your Modal is not bordered</p>
        </Modal>
      )
    }}
  </Story>
</Canvas>

## with disclosure

<Canvas>
  <Story name="disclosure">
    <Modal disclosure={({ baseId }) => <Button> open {baseId} </Button>}>
      Content
    </Modal>
  </Story>
</Canvas>

## with function children

<Canvas>
  <Story name="children function">
    <Modal disclosure={() => <Button>modal</Button>}>
      {props => <p>Content {props.baseId} </p>}
    </Modal>
  </Story>
</Canvas>

<ArgsTable of={Modal} />
