import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'
import Reminder from '..'

<Meta title="Components/Feedback/Reminder" component={Reminder} />

# Reminder

<Description>
  A styled badge with extra feature. In the `text` prop place some text between
  `[]` to make it bolder
</Description>

<Canvas>
  <Story name="Basic">
    <Reminder display="flex" mb={1} text="Your credit card [has expired]" />
  </Story>
</Canvas>

## Variants

Use `variant` prop to change the style of the component.

<Canvas>
  <Story name="Variants">
    <Reminder mb={1} variant="error" text="error" />
    <Reminder mb={1} variant="warning" text="warning" />
    <Reminder mb={1} variant="info" text="info" />
    <Reminder mb={1} text="default" />
  </Story>
</Canvas>

## Link

Use `to` prop to make this component act as a link.

<Canvas>
  <Story name="Link">
    <Reminder
      to="."
      mb={1}
      variant="error"
      text="Your credit card [has expired]"
    />
    <Reminder
      to="."
      mb={1}
      variant="warning"
      text="You have [1 locked instance]"
    />
    <Reminder to="." mb={1} variant="info" text="[2FA] is deactivated" />
    <Reminder to="." mb={1} text="No [phone number] registered" />
  </Story>
</Canvas>

## Bordered

Add `bordered` prop to display a border around the component.

<Canvas>
  <Story name="Bordered">
    <Reminder
      to="/"
      mb={1}
      bordered
      variant="error"
      text="Your credit card [has expired]"
    />
    <Reminder
      to="/"
      mb={1}
      bordered
      variant="warning"
      text="You have [1 locked instance]"
    />
    <Reminder
      to="/"
      mb={1}
      bordered
      variant="info"
      text="[2FA] is deactivated"
    />
    <Reminder to="/" mb={1} bordered text="No [phone number] registered" />
  </Story>
</Canvas>

## API

<ArgsTable of={Reminder} />
