import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { Reminder } from '../'

<Meta title="Components/Reminder" component={Reminder} />

# Reminder

## Basic

<Canvas>
  <Story name="error">
    <Reminder
      onClick={() => alert('clicked')}
      mb={1}
      variant="error"
      text="Your credit card [has expired]"
    />
  </Story>
  <Story name="warning">
    <Reminder
      onClick={() => alert('clicked')}
      mb={1}
      variant="warning"
      text="You have [1 locked instance]"
    />
  </Story>
  <Story name="info">
    <Reminder
      onClick={() => alert('clicked')}
      mb={1}
      variant="info"
      text="[2FA] is deactivated"
    />
  </Story>
  <Story name="default">
    <Reminder
      onClick={() => alert('clicked')}
      mb={1}
      text="No [phone number] registered"
    />
  </Story>
</Canvas>

## As link

<Canvas>
  <Story name="link-error">
    <Reminder
      to="."
      mb={1}
      variant="error"
      text="Your credit card [has expired]"
    />
  </Story>
  <Story name="link-warning">
    <Reminder
      to="."
      mb={1}
      variant="warning"
      text="You have [1 locked instance]"
    />
  </Story>
  <Story name="link-info">
    <Reminder to="." mb={1} variant="info" text="[2FA] is deactivated" />
  </Story>
  <Story name="link-default">
    <Reminder to="." mb={1} text="No [phone number] registered" />
  </Story>
</Canvas>

## Bordered

<Canvas>
  <Story name="bordered-error">
    <Reminder
      to="/"
      mb={1}
      bordered
      variant="error"
      text="Your credit card [has expired]"
    />
  </Story>
  <Story name="bordered-warning">
    <Reminder
      to="/"
      mb={1}
      bordered
      variant="warning"
      text="You have [1 locked instance]"
    />
  </Story>
  <Story name="bordered-info">
    <Reminder
      to="/"
      mb={1}
      bordered
      variant="info"
      text="[2FA] is deactivated"
    />
  </Story>
  <Story name="bordered-default">
    <Reminder to="/" mb={1} bordered text="No [phone number] registered" />
  </Story>
</Canvas>
