import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs/blocks'
import BorderedBox from '..'

<Meta title="Components/BorderedBox" component={BorderedBox} />

# BorderedBox

<Canvas>
  <Story name="basic">
    <BorderedBox>Basic BorderedBox</BorderedBox>
  </Story>
</Canvas>

## Custom component

Set a custom base component using `as` prop.

<Canvas>
  <Story name="Custom component">
    <BorderedBox as="span">BorderedBox as span</BorderedBox>
  </Story>
</Canvas>

## Link

<Canvas>
  <Story name="As link">
    <BorderedBox href="#" m={1}>
      Href Link
    </BorderedBox>
    <BorderedBox to="/" m={1}>
      To Link
    </BorderedBox>
  </Story>
</Canvas>

## Variants

<Canvas>
  <Story name="Horizontal">
    <BorderedBox variant="horizontal">Horizontal</BorderedBox>
  </Story>
  <Story name="Vertical">
    <BorderedBox variant="vertical">Vertical</BorderedBox>
  </Story>
</Canvas>

## API

<ArgsTable of={BorderedBox} />
