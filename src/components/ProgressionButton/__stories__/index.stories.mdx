import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import { ProgressionButton } from '..'

<Meta title="Components/ProgressionButton" component={ProgressionButton} />

# ProgressionButton

<Description>An horizontal progress bar.</Description>

## Basic

<Canvas>
  <Story name="basic">
    <ProgressionButton height={40}>Progressing…</ProgressionButton>
  </Story>
</Canvas>

## Custom component

<Description>
  The `creation` props indicates the starting time of the progression, by
  default it&apos;s the time of when the object is mounted.
</Description>
<Description>
  The `duration` props indicates an approximation of the progression duration in
  seconds, by default it&apos;s `120`
</Description>
<Description>
  You can chose the color of the Progression using the `color` props
</Description>

<Canvas isColumn>
  <Story name="custom-not-started">
    <ProgressionButton
      creation={new Date(new Date().setSeconds(new Date().getSeconds() + 10))}
      duration={20}
      color="beta"
      height={40}
    >
      I&apos;m going to start in 10 seconds and take 20 seconds to complete
    </ProgressionButton>
  </Story>
  <Story name="custom-slow">
    <ProgressionButton
      creation={new Date(new Date().setSeconds(new Date().getSeconds() - 60))}
      duration={90}
      color="primary"
      height={40}
    >
      I was supposed to start one minute ago and take 90 seconds (meaning I only
      have 30s left and start at 66%)
    </ProgressionButton>
  </Story>
  <Story name="custom-already-done">
    <ProgressionButton
      creation={new Date(new Date().setSeconds(new Date().getSeconds() - 90))}
      duration={90}
      color="info"
      height={40}
    >
      I was supposed to start 90 seconds ago and take 90 seconds (meaning
      I&apos;m already done)
    </ProgressionButton>
  </Story>
</Canvas>

<ArgsTable of={ProgressionButton} />
