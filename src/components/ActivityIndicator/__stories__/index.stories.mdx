import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import { ActivityIndicator } from '..'

<Meta
  title="Components/ActivityIndicator"
  component={ActivityIndicator}
  argTypes={{
    color: {
      control: {
        type: 'string',
      },
    },
    size: {
      control: {
        type: 'number',
      },
    },
  }}
/>

# ActivityIndicator

<Description of={ActivityIndicator}>
  Component that indicate a loading period.
</Description>

## Basic

<Canvas>
  <Story name="default">
    <ActivityIndicator size={24} />
  </Story>
</Canvas>

## Size

<Canvas>
  <Story
    name="40"
    args={{
      size: 40,
    }}
  >
    {args => <ActivityIndicator {...args} />}
  </Story>
  <Story
    name="50"
    args={{
      size: 50,
    }}
  >
    {args => <ActivityIndicator {...args} />}
  </Story>
  <Story
    name="60"
    args={{
      size: 60,
    }}
  >
    {args => <ActivityIndicator {...args} />}
  </Story>
</Canvas>

## Color

<Canvas>
  <Story
    name="primary"
    args={{
      color: 'primary',
      size: 24,
    }}
  >
    {args => <ActivityIndicator {...args} />}
  </Story>
  <Story
    name="success"
    args={{
      color: 'success',
      size: 24,
    }}
  >
    {args => <ActivityIndicator {...args} />}
  </Story>
  <Story
    name="warning"
    args={{
      color: 'warning',
      size: 24,
    }}
  >
    {args => <ActivityIndicator {...args} />}
  </Story>
</Canvas>

<ArgsTable of={ActivityIndicator} />
