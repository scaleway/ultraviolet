import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import ActivityIndicator from '..'
import Box from '../../Box'

<Meta title="Components/ActivityIndicator" component={ActivityIndicator} />

# ActivityIndicator

<Description>Component that indicate a loading period.</Description>

<Canvas>
  <Story name="Basic">
    <ActivityIndicator size={24} />
  </Story>
</Canvas>

## Size

<Description>
  You can set the size of the component with the `size` prop.
</Description>

<Canvas>
  <Story name="Size">
    <Box display="flex" alignItems="center">
      <ActivityIndicator size={40} />
      <ActivityIndicator size={60} mx={1} />
      <ActivityIndicator size={80} />
    </Box>
  </Story>
</Canvas>

## Color

You can set the color of the component by using the `color` prop. You can use theme color or a custom one.

<Canvas>
  <Story name="Color">
    <Box display="flex">
      <ActivityIndicator mr={1} size={24} color="warning" />
      <ActivityIndicator size={24} color="#000" />
    </Box>
  </Story>
</Canvas>

<ArgsTable of={ActivityIndicator} />
