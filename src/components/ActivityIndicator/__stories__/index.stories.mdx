import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import { colors } from '../../../theme'
import ActivityIndicator from '../index'

<Meta title="Components/ActivityIndicator" component={ActivityIndicator} />

# ActivityIndicator

<Description>
  A component representing percentage as a circle which can be used to show
  activity
</Description>

## Basic

<Canvas>
  <Story name="Basic">
    <ActivityIndicator percentage={75} />
  </Story>
</Canvas>

## Percentage

<Description>
  You can set the percentage of completion with the `percentage` prop.
</Description>

<Canvas>
  <Story name="Percentage">
    {[8, 32, 50, 75, 90, 100].map(percentage => (
      <div style={{ display: 'inline-flex', marginRight: 8 }}>
        <ActivityIndicator
          key={`percent-${percentage}`}
          percentage={percentage}
        />
      </div>
    ))}
  </Story>
</Canvas>

## Size

<Description>
  You can set the size of the component with the `size` prop.
</Description>

<Canvas>
  <Story name="Size">
    {[8, 16, 24, 32, 40, 100].map(size => (
      <div style={{ display: 'inline-flex', marginRight: 8 }}>
        <ActivityIndicator key={`size-${size}`} percentage={75} size={size} />
      </div>
    ))}
  </Story>
</Canvas>

## Color

<Description>
  You can set the color of the component by using the `color` prop. You can use
  theme color or a custom one.
</Description>

<Canvas>
  <Story name="Color">
    {Object.keys(colors).map(color => (
      <div style={{ display: 'inline-flex', marginRight: 8 }}>
        <ActivityIndicator
          key={`color-${color}`}
          color={color}
          percentage={75}
        />
      </div>
    ))}
  </Story>
</Canvas>

## Trail Color

<Description>
  You can set the trail color (background) of the component by using the
  `trailColor` prop. You can use theme color or a custom one.
</Description>

<Canvas>
  <Story name="Trail Color">
    {['white', 'gray300', 'gray550', 'zumthor', 'shadow', 'beta', 'gold'].map(
      color => (
        <div style={{ display: 'inline-flex', marginRight: 8 }}>
          <ActivityIndicator
            key={`trailColor-${color}`}
            trailColor={color}
            percentage={75}
          />
        </div>
      ),
    )}
  </Story>
</Canvas>

## Stroke Width

<Description>
  You can also set the stroke width with the `strokeWidth` props
</Description>

<Canvas>
  <Story name="Stroke Width">
    {[8, 16, 24, 32, 40, 100].map(size => (
      <div style={{ display: 'inline-flex', marginRight: 8 }}>
        <ActivityIndicator
          key={`strokeWidth-${size}`}
          percentage={75}
          strokeWidth={size}
        />
      </div>
    ))}
  </Story>
</Canvas>

## Text

<Description>
  You can pass a text which will be inlined in the center of the circle with the
  `text` props
</Description>

<Canvas>
  <Story name="Text">
    {[8, 32, 50, 75, 90, 100].map(percentage => (
      <div style={{ display: 'inline-flex', marginRight: 8 }}>
        <ActivityIndicator
          key={`text-${percentage}`}
          percentage={percentage}
          text={`${percentage}%`}
          display="inline-flex"
        />
      </div>
    ))}
  </Story>
</Canvas>

## Active

<Description>
  You can set the `active` prop to indicate to set the indicator active
</Description>
<Description>
  The percentage is set at `20` by default when active but you can override it
</Description>

<Canvas>
  <Story name="Active">
    {[10, 20, 30, 40, 50, 100].map(size => (
      <div style={{ display: 'inline-flex', marginRight: 8 }}>
        <ActivityIndicator key={`active-${size}`} size={size} active />
      </div>
    ))}
  </Story>
</Canvas>

<ArgsTable of={ActivityIndicator} />
