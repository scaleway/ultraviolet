import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import NotificationBar, { notificationBarVariants } from '..'
import { Box } from '../..'

<Meta title="Components/NotificationBar" component={NotificationBar} />

# NotificationBar

<Description>Displays a notice with an icon.</Description>

<Canvas>
  <Story name="Basic">
    <NotificationBar>
      This is a notification bar with its default props.
    </NotificationBar>
  </Story>
</Canvas>

## Variants

<Description>
  Using `variant` prop you can change the look of the component.
</Description>

<Canvas>
  <Story name="Variants">
    {notificationBarVariants.map(variant => (
      <Box key={variant} my={1}>
        <NotificationBar variant={variant}>
          This is a notification bar with the {variant} variant.
        </NotificationBar>
      </Box>
    ))}
  </Story>
</Canvas>

## Icon

<Description>
  Using `icon` prop you can add a custom icon to the notification.
</Description>

<Canvas>
  <Story name="Icon">
    <NotificationBar icon="clock-outline">
      This is a notification bar with a custom icon.
    </NotificationBar>
  </Story>
</Canvas>

## Icon size

<Description>Using `iconSize` prop you can change the icon size.</Description>

<Canvas>
  <Story name="Icon size">
    <NotificationBar iconSize={40}>
      This is a notification bar with a custom icon size.
    </NotificationBar>
  </Story>
</Canvas>

## API

<ArgsTable of={NotificationBar} />
