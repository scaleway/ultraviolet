import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'
import FlexBox, { Child } from '..'

<Meta title="Components/Foundation/FlexBox" component={FlexBox} />

# FlexBox

<Description>
  `FlexBox` a flex container, it accepts the same props as the css spec
</Description>

<Canvas>
  <Story name="Basic">
    <FlexBox>I'm a flexbox</FlexBox>
  </Story>
</Canvas>

<ArgsTable of={FlexBox} />

## Child

<Description>FlexBox.Child represent a flex children</Description>

<Canvas>
  <Story name="Child">
    <FlexBox justifyContent="space-between">
      <FlexBox.Child>First Child</FlexBox.Child>
      <FlexBox.Child>Second Child</FlexBox.Child>
    </FlexBox>
  </Story>
</Canvas>

<ArgsTable of={Child} />

## Nested FlexBox

<Description>
  A `FlexBox` also accepts `FlexBox.Child` props in order to be able to be
  nested
</Description>

<Canvas>
  <Story name="Nested">
    <FlexBox justifyContent="space-between">
      <FlexBox flex={1}>
        <FlexBox.Child flex={1}>First Nested Child</FlexBox.Child>
        <FlexBox.Child flex={1}>Second Nested Child</FlexBox.Child>
      </FlexBox>
      <FlexBox.Child flex={1} order={-1}>
        Second Child (with custom order)
      </FlexBox.Child>
    </FlexBox>
  </Story>
</Canvas>
