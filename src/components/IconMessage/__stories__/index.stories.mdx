import { Canvas, Description, Meta, Story } from '@storybook/addon-docs/blocks'
import { IconMessage } from '..'

<Meta
  title="Components/IconMessage"
  component={IconMessage}
  argTypes={{
    variant: {
      control: {
        type: 'select',
        options: ['warning', 'info', 'success'],
      },
    },
    icon: {
      control: {
        type: 'string',
      },
    },
  }}
/>

# IconMessage

<Description>
  Set color using `variant` property with the same keywords of _variant_ colors
  in `src/theming/colors.js` file. Set icon using `icon` property with the same
  keywords of @scaleway/ui `Icon` component. Set texts using an array in `texts`
  property. Each entry will be placed in a `Typography as="p"` component.
</Description>

## `variant`

<Canvas>
  <Story
    name="warning"
    args={{
      variant: 'warning',
      icon: 'alert',
    }}
  >
    {args => (
      <IconMessage {...args}>
        LOREMINEM IPSUM
        <br />
        The way she moves, she's like a belly-dancer. She's shaking that ass to
        the new Nelly jams. I think someone's at the door but I don't think
        I'mma answer.
      </IconMessage>
    )}
  </Story>
  <Story
    name="info"
    args={{
      variant: 'info',
      icon: 'information-outline',
    }}
  >
    {args => (
      <IconMessage {...args}>
        If you could count the skeletons in my closet. Under my bed and up under
        my faucet. Then you would know I've completely lost it.
      </IconMessage>
    )}
  </Story>
  <Story
    name="success"
    args={{
      variant: 'success',
      icon: 'check',
    }}
  >
    {args => (
      <IconMessage {...args}>
        LOREMINEM IPSUM
        <br />
        When you walked through the door. It was clear to me (Clear to me).
        You’re the one they adore. Who they came to see (Who they came to see).
        You’re a rock star (Baby). Everybody wants you (Everybody wants you).
        Player, who can really blame you? (Who can really blame you?). We’re the
        ones who made you.
      </IconMessage>
    )}
  </Story>
</Canvas>
