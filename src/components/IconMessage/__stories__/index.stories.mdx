import { Canvas, Description, Meta, Story } from '@storybook/addon-docs'
import IconMessage from '..'

<Meta
  title="Components/IconMessage"
  component={IconMessage}
  argTypes={{
    icon: {
      control: {
        type: 'string',
      },
    },
    variant: {
      control: {
        options: ['warning', 'info', 'success'],
        type: 'select',
      },
    },
  }}
/>

# IconMessage

<Description>
  Set color using `variant` property with the same keywords of _variant_ colors
  in `src/theming/colors.js` file. Set icon using `icon` property with the same
  keywords of @scaleway/ui `Icon` component. Set texts using an array in `texts`
  property. Each entry will be placed in a `Typography as=&quot;p&quot;`
  component.
</Description>

## `variant`

<Canvas>
  <Story
    name="warning"
    args={{
      icon: 'alert',
      variant: 'warning',
    }}
  >
    {args => (
      <IconMessage {...args}>
        LOREMINEM IPSUM
        <br />
        The way she moves, she&apos;s like a belly-dancer. She&apos;s shaking
        that ass to the new Nelly jams. I think someone&apos;s at the door but I
        don&apos;t think I&apos;mma answer.
      </IconMessage>
    )}
  </Story>
  <Story
    name="info"
    args={{
      icon: 'information-outline',
      variant: 'info',
    }}
  >
    {args => (
      <IconMessage {...args}>
        If you could count the skeletons in my closet. Under my bed and up under
        my faucet. Then you would know I&apos;ve completely lost it.
      </IconMessage>
    )}
  </Story>
  <Story
    name="success"
    args={{
      icon: 'check',
      variant: 'success',
    }}
  >
    {args => (
      <IconMessage {...args}>
        LOREMINEM IPSUM
        <br />
        When you walked through the door. It was clear to me (Clear to me).
        You&apos;re the one they adore. Who they came to see (Who they came to
        see). You&apos;re a rock star (Baby). Everybody wants you (Everybody
        wants you). Player, who can really blame you? (Who can really blame
        you?). We&apos;re the ones who made you.
      </IconMessage>
    )}
  </Story>
</Canvas>
