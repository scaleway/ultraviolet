import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs/blocks'
import { format } from 'date-fns'
import LineChart from '..'
import {
  lineChartData,
  lineChartHoursData,
  lineChartMultipleData,
} from './mockData'

<Meta title="Components/Data Display/LineChart" />

# LineChart

## One range of data

### Basic linear use

<Canvas>
  <Story name="basic linear">
    <LineChart data={lineChartData} xScale={{ type: 'linear' }} />
  </Story>
</Canvas>

### Basic time use

<Canvas>
  <Story name="basic hours">
    <LineChart
      data={lineChartHoursData}
      axisFormatters={{
        bottom: value => format(value, 'dd-MM-y'),
      }}
    />
  </Story>
</Canvas>

### Formatted axis and points

<Canvas>
  <Story name="formatted">
    <LineChart
      data={lineChartHoursData}
      axisFormatters={{
        bottom: value => format(value, 'dd-MM'),
        left: value => `${value} liters`,
      }}
      pointFormatters={{
        x: value => format(value, 'dd-MM-y hh:mm'),
        y: value => `${value} liters`,
      }}
    />
  </Story>
</Canvas>

## Multiple range of data

### Multiple series with custom legend

<Canvas>
  <Story name="multiple">
    <LineChart
      data={lineChartMultipleData}
      axisFormatters={{
        bottom: value => format(value, 'dd-MM-y'),
      }}
      withLegend
    />
  </Story>
</Canvas>

<ArgsTable of={LineChart} />
