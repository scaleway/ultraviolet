import { Meta, Story, Canvas, Description } from '@storybook/addon-docs/blocks'
import { Badge } from '..'

<Meta
  title="Components/Badge"
  component={Badge}
  argTypes={{
    variant: {
      control: {
        type: 'select',
        options: ['success', 'beta', 'warning', 'neutral'],
      },
    },
    size: {
      control: {
        type: 'select',
        options: ['rounded', 'md', 'sm', 'xs', 'xxs'],
      },
    },
  }}
/>

# Badge

<Description>Highlights information.</Description>

<Canvas>
  <Story name="default" args={{}}>
    {args => <Badge {...args}>A Badge</Badge>}
  </Story>
</Canvas>

## `variant`

#### `neutral`
<Canvas>
  <Story
    name="neutral"
    args={{
      variants: [
        'success',
        'beta',
        'error',
        'info',
        'neutral',
      ],
    }}
  >
    {args => args.variants.map(variant => <Badge variant={variant} mx={1}>{variant}</Badge>)}
  </Story>
</Canvas>

#### `lighter`
<Canvas>
  <Story
    name="light"
    args={{
      variants: [
        'light-success',
        'light-beta',
        'light-error',
        'light-info',
        'light-neutral',
      ],
    }}
  >
    {args => args.variants.map(variant => <Badge variant={variant} mx={1}>{variant}</Badge>)}
  </Story>
</Canvas>

## `size`

<Canvas>
  <Story
    name="md"
    args={{
      sizes: [
        'md',
        'sm',
        'xs',
        'xxs',
        'rounded'
      ]
    }}
  >
    {args => args.sizes.map(size => <Badge size={size} mx={1}>{size}</Badge>)}
  </Story>
</Canvas>
