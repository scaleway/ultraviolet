import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs/blocks'
import { Badge } from '..'
import { Box } from '../../Box'

<Meta
  title="Components/Badge"
  component={Badge}
  argTypes={{
    variant: {
      control: {
        type: 'select',
        options: ['success', 'beta', 'warning', 'neutral'],
      },
    },
    size: {
      control: {
        type: 'select',
        options: ['rounded', 'medium', 'small', 'xsmall', 'xxsmall'],
      },
    },
  }}
/>

# Badge

## Basic

<Canvas>
  <Story name="Basic" args={{}}>
    {args => <Badge {...args}>A Badge</Badge>}
  </Story>
</Canvas>

## Variants

<Canvas>
  <Story
    name="Variants"
    args={{
      variants: ['primary', 'success', 'beta', 'error', 'info', 'neutral'],
      variantsLight: [
        'light-primary',
        'light-success',
        'light-beta',
        'light-error',
        'light-info',
        'light-neutral',
      ],
    }}
  >
    {args => (
      <>
        <Box mb={2}>
          {args.variants.map(variant => (
            <Badge key={variant} variant={variant} mx={1}>
              {variant}
            </Badge>
          ))}
        </Box>
        <Box>
          {args.variantsLight.map(light => (
            <Badge key={light} variant={light} mx={1}>
              {light}
            </Badge>
          ))}
        </Box>
      </>
    )}
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story
    name="Sizes"
    args={{
      sizes: ['medium', 'small', 'xsmall', 'xxsmall', 'rounded'],
    }}
  >
    {args =>
      args.sizes.map(size => (
        <Badge key={size} size={size} mx={1}>
          {size}
        </Badge>
      ))
    }
  </Story>
</Canvas>

## API

<ArgsTable of={Badge} />
