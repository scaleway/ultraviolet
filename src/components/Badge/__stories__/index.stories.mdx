import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs/blocks'
import Badge from '..'

<Meta
  title="Components/Badge"
  component={Badge}
  argTypes={{
    size: {
      control: {
        options: ['rounded', 'medium', 'small', 'xsmall', 'xxsmall'],
        type: 'select',
      },
    },
    variant: {
      control: {
        options: ['success', 'beta', 'warning', 'neutral'],
        type: 'select',
      },
    },
  }}
/>

# Badge

<Canvas>
  <Story name="Basic" args={{}}>
    {args => <Badge {...args}>A Badge</Badge>}
  </Story>
</Canvas>

## Variants

<Canvas>
  <Story
    name="Variants"
    args={{
      variants: ['primary', 'success', 'beta', 'error', 'info', 'neutral'],
      variantsLight: [
        'light-primary',
        'light-success',
        'light-beta',
        'light-error',
        'light-info',
        'light-neutral',
      ],
    }}
  >
    {args => (
      <>
        <div>
          {args.variants.map(variant => (
            <Badge m={1} key={variant} variant={variant}>
              {variant}
            </Badge>
          ))}
        </div>
        <div>
          {args.variantsLight.map(light => (
            <Badge m={1} key={light} variant={light}>
              {light}
            </Badge>
          ))}
        </div>
      </>
    )}
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story
    name="Sizes"
    args={{
      sizes: ['medium', 'small', 'xsmall', 'xxsmall', 'rounded'],
    }}
  >
    {args =>
      args.sizes.map(size => (
        <Badge key={size} size={size} mx={1}>
          {size}
        </Badge>
      ))
    }
  </Story>
</Canvas>

## API

<ArgsTable of={Badge} />
