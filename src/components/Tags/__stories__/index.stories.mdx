import { Meta, Story, Canvas, Description } from '@storybook/addon-docs/blocks'
import { Tags } from '..'

<Meta title="Components/Tags" component={Tags} />

# Tags

## Basic

<Description>
  `Open your console to check onChange event`
</Description>

<Canvas>
  <Story name="basic">
    <Tags onChange={tags => console.log(tags)} tags={['Smooth', 'code']} />
  </Story>
</Canvas>

## Variant Bordered

<Canvas>
  <Story name="bordered">
    <Tags
      variant="bordered"
      onChange={tags => console.log(tags)}
      tags={['Smooth', 'code']}
    />
  </Story>
</Canvas>

## Custom placeholder

<Canvas>
  <Story name="placeholder">
    <Tags placeholder="Please type your tags" onChange={tags => console.log(tags)} />
  </Story>
</Canvas>

## With a custom error handler

<Canvas>
  <Story name="error-handler">
    <Tags
      onChangeError={error => {
        console.log(error)
      }}
      onChange={tags => {
        throw new Error('')
      }}
      tags={['Smooth', 'code']}
    />
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="disabled">
    <Tags disabled onChange={tags => console.log(tags)} tags={['Smooth', 'code']} />
  </Story>
</Canvas>
