import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'
import { useState } from 'react'
import DotSteps from '..'

<Meta title="Components/Navigation/DotSteps" component={DotSteps} />

# DotSteps

<Description>Dot Steps control, to switch between pages/steps.</Description>

<Canvas>
  <Story name="Basic" args={{ steps: 2 }}>
    {() => {
      const [step, setStep] = useState(1)
      return (
        <>
          {step === 1 && 'Content of 1st step'}
          {step === 2 && 'Content of the 2nd step'}
          <DotSteps {...{ setStep, step }} />
        </>
      )
    }}
  </Story>
</Canvas>

## API

<ArgsTable of={DotSteps} />
