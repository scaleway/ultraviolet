import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import MarkDown from '..'
import { html, simple, withMultipleParagraphs } from './markdown_example'

<Meta title="Components/MarkDown" component={MarkDown} />

# MarkDown

<Description>
  A wrapper for [react-markdown](https://github.com/remarkjs/react-markdown)
</Description>

<Canvas>
  <Story name="Basic">
    <MarkDown source={simple} linkTarget="_blank" />
  </Story>
</Canvas>

## Inline

<Description>
  You can make the paragraphs inside the source render inline by setting the
  `inline prop`
</Description>

<Canvas>
  <Story name="Paragraphs">
    <MarkDown source={withMultipleParagraphs} />
  </Story>
</Canvas>

<Canvas>
  <Story name="Paragraphs inline">
    <MarkDown source={withMultipleParagraphs} inline />
  </Story>
</Canvas>

## Link Target

<Description>
  `linkTarget` prop allows you to specify the target props for the links inside
  your markdown text. By default in react-markdown links are opened in a new
  tab.
</Description>

<Canvas>
  <Story name="Link Target">
    <MarkDown source={simple} linkTarget="_self" />
  </Story>
</Canvas>

## Escape HTML

<Description>`escapeHtml` manages the html escape of your markdown</Description>

<Canvas>
  <Story name="Escape HTML">
    <MarkDown source={html} escapeHtml={false} />
  </Story>
</Canvas>

## API

<ArgsTable of={MarkDown} />
