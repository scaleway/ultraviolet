import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs/blocks'
import CreationProgress from '..'
import ControlValue from '../../../__stories__/components/ControlValue'
import { Button } from '../../Button'

<Meta title="Components/CreationProgress" component={CreationProgress} />

# CreationProgress

<Description>CreationProgress //TODO:</Description>

<Canvas>
  <Story name="Basic">
    <CreationProgress selected={0}>
      <CreationProgress.Step>Step 1</CreationProgress.Step>
      <CreationProgress.Step>Step 2</CreationProgress.Step>
      <CreationProgress.Step>Step 3</CreationProgress.Step>
    </CreationProgress>
  </Story>
</Canvas>

<Canvas>
  <Story name="Selected 1">
    <CreationProgress selected={1}>
      <CreationProgress.Step>Step 1</CreationProgress.Step>
      <CreationProgress.Step>Step 2</CreationProgress.Step>
      <CreationProgress.Step>Step 3</CreationProgress.Step>
    </CreationProgress>
  </Story>
</Canvas>

<Canvas>
  <Story name="Selected all">
    <CreationProgress selected={2}>
      <CreationProgress.Step>Step 1</CreationProgress.Step>
      <CreationProgress.Step>Step 2</CreationProgress.Step>
      <CreationProgress.Step>Step 3</CreationProgress.Step>
    </CreationProgress>
  </Story>
</Canvas>

# Without animation

<Canvas>
  <Story name="No animation">
    <CreationProgress selected={1} animated={false}>
      <CreationProgress.Step>Step 1</CreationProgress.Step>
      <CreationProgress.Step>Step 2</CreationProgress.Step>
      <CreationProgress.Step>Step 3</CreationProgress.Step>
    </CreationProgress>
  </Story>
</Canvas>

# With steps number

By choosing `number` variant with `variant="number"` prop, step are numbered

<Canvas>
  <Story name="numbered">
    <CreationProgress selected={1} variant="number">
      <CreationProgress.Step>Step 1</CreationProgress.Step>
      <CreationProgress.Step>Step 2</CreationProgress.Step>
      <CreationProgress.Step>Step 3</CreationProgress.Step>
    </CreationProgress>
  </Story>
</Canvas>

# Sizes

You can change the size of the creation progress with the `size` props which accept `xsmall`, `small`, `medium`, `large`, `xlarge`. By default `xlarge` will be taken

<Canvas>
  <Story name="Sizes">
    <ControlValue value={{ animated: true, variant: 'default' }}>
      {({ value, onChange }) => (
        <>
          <div>
            <Button
              mr={1}
              mb={1}
              onClick={() =>
                onChange({
                  ...value,
                  variant: value.variant === 'number' ? 'default' : 'number',
                })
              }
            >
              Toggle variant
            </Button>
            <Button
              mb={1}
              onClick={() =>
                onChange({ ...value, animated: !value.animated })
              }
            >
              Toggle animated
            </Button>
          </div>
          {['xsmall', 'small', 'medium', 'large', 'xlarge'].map(size => (
            <CreationProgress
              size={size}
              mb={1}
              selected={1}
              animated={value.animated}
              variant={value.variant}
            >
              <CreationProgress.Step>Step 1</CreationProgress.Step>
              <CreationProgress.Step>Step 2</CreationProgress.Step>
              <CreationProgress.Step>Step 3</CreationProgress.Step>
            </CreationProgress>
          ))}
        </>
      )}
    </ControlValue>
  </Story>
</Canvas>

# Styling

You can pass all props used by `xstyled` to customize the container of the component

<Canvas>
  <Story name="Styling">
    <CreationProgress width="50%" size="small" selected={1} isStepsNumber>
      <CreationProgress.Step>Step 1</CreationProgress.Step>
      <CreationProgress.Step>Step 2</CreationProgress.Step>
      <CreationProgress.Step>Step 3</CreationProgress.Step>
    </CreationProgress>
  </Story>
</Canvas>

<ArgsTable of={CreationProgress} />
