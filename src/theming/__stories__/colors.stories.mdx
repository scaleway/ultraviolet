import { Meta, Story } from '@storybook/addon-docs/blocks'
import { Box, Typography } from '../..'
import * as colorsReference from '../colors'

<Meta title="Theme/Colors" />

# Colors

<Story name="colors">
  {Object.keys(colorsReference).map((color) => (
      color !== 'colors' && (
        <Box display="flex" justifyContent="center" key={color}>
          <Box
            textAlign="center"
            width={100}
            p={1}
            m={1}
            backgroundColor="black"
          >
            <Typography color={color}>{color}</Typography>
          </Box>
          <Box
            textAlign="center"
            width={100}
            p={1}
            m={1}
            backgroundColor="white"
          >
            <Typography color={color}>{color}</Typography>
          </Box>
          <Box
            textAlign="center"
            width={100}
            p={1}
            m={1}
            backgroundColor={color}
          >
            <Typography color="white">{color}</Typography>
          </Box>
          <Box
            textAlign="center"
            width={100}
            p={1}
            m={1}
            backgroundColor={color}
          >
            <Typography color="black">{color}</Typography>
          </Box>
        </Box>
      )
    ))}
</Story>
